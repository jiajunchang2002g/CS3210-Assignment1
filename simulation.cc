
#include <assert.h>
#include <omp.h>

#include <algorithm>
#include <iostream>
#include <optional>
#include <vector>

#include "common.h"

namespace traffic_prng {
extern PRNG* engine;
}

void executeSimulation(Params params, std::vector<Car> cars) {
  
}